<div class="title-category-index">
  <h2>Category</h2>
</div>

<div class="row list-categories">
  <% @categories.each do |category| %>
      <div class="category-name">
        <%= link_to category.name, category %>
      </div>
  <% end %>
</div>

<%= render 'image_frame', images: @images %>